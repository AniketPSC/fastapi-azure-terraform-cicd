locals {
  rg_name        = "${var.prefix}-rg"
  acr_name       = "${var.prefix}acr"
  app_name       = "${var.prefix}-app"
  key_vault_name = "${var.prefix}-kv"
  plan_name      = "${var.prefix}-plan"
}
